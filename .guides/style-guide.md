# Making/Editing layouts
- Use flex and gap as much as possible.
- Don't use complex styles like `shrink-[]`, `leading-[]`, `flex-[]` until necessary.
- When using icons inside `Button`, don't give margin to icon, `gap` is already set on the `Button`.
- For Icons, use `size-{x}` instead of `w-{x} h-{x}`.